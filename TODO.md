# TODOs
* [ ] implement function to get available reference genomes for regions to load

## optional
* [ ] use `base::delayedAssign` for lazy loading in `load_genomic_regions()`

## done
* [x] general human and mouse genome metadata handles (e.g. human_centromeres)
* [x] export key datasets
